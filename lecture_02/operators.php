<?php


    // greater than
    if( 5 > 4 ){}

    // less than
    if( 5 > 4 ){}

    // greater than equal to
    if( 5 >= 4){}

    // less than equal to
    if( 5 <= 4 ){}

    // modulus
    if( 5 % 5 == 0 ){}
    /**
     *
     * Divide 5 by 5
     * Check remainder
     * 
     * Above example checks 
     * no remainder
     *
     **/

    // and
    if( 5 > 4 && 5 > 2 ){}

    // or
    if( 5 > 4 || 5 < 2 ){}

    // xor
    if( true xor false ){}
    /**
     *
     * If either is true but not both
     *
     **/

    // not (reverse condition)
    if( ! true ){}


?>
