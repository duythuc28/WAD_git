/*
	attributes: returns all attributes for this node
	childNodes: return a all child Nodes
	firstChild: return the first child of this node
	lastChild: return the last child of this node
	nextSibling: return the next sibling node
	nodeName: return the node Name
	nodeType: return the node type
	nodeValue: returns or sets the value of this node
	parentNode: return the parent node of this node
	previousSibling: returns the previous sibling


	[Node Object Methods]

    - appendChild(newChild)
      Appends the node newChild at the end of the child nodes for this node

    - cloneNode(boolean)
      Returns an exact clone of this node. If the boolean value is set to true, the cloned node contains all the child nodes as well

    - hasChildNodes()
      Returns true if this node has any child nodes

    - insertBefore(newNode,refNode)
      Inserts a new node, newNode, before the existing node, refNode

    - removeChild(nodeName)
      Removes the specified node, nodeName

    - replaceChild(newNode,oldNode)
      Replaces the oldNode, with the newNode

    - setAttributeNode(newAttr)
      Insert the new attribute newAttr to the node
*/